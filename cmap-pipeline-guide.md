# CMap pipeline guide — moved

This standalone guide has been superseded. The canonical, up-to-date version lives
in the docs site:

➡️ **[`docs/pipeline.md`](docs/pipeline.md)** — Building the database (GCTX → DuckDB),
now covering all three perturbation classes (drug / knockdown / overexpression),
the per-class `data/processed/<class>/` layout, and the Zenodo per-class archives.

See also:
- **[`docs/data-release.md`](docs/data-release.md)** — depositing the data on Zenodo.
- **[`README.md`](README.md)** — project overview and the three-tool model.

> This file is kept only as a redirect so existing links don't 404; it can be
> removed once nothing points at `cmap-pipeline-guide.md`.
